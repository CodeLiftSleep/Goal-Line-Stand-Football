<Page x:Class="DraftPlayersTable"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFGeneration"
        mc:Ignorable="d"
        Title="DraftPlayersTable" Height="{Binding Source={x:Static SystemParameters.WorkArea}, Path = Height}" Width="{Binding Source={x:Static SystemParameters.WorkArea}, Path = Width}"
        >
    <Grid Height="Auto" Width="Auto">
        <Grid.Background>
            <ImageBrush ImageSource="Content/Images/JENK10.png" Opacity="0.7"></ImageBrush>
        </Grid.Background>
        <DataGrid x:Name="DraftPlayersDataGrid" ItemsSource="{Binding DraftPlayers}" />
    </Grid>
</Page>
